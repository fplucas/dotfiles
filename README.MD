# fplucas's dotfiles

These are my dotfiles. Used in an Archlinux machine.

### Programs which have configs in this repository
- dunst (notification daemon)
- neovim (text editor)
- ranger (file manager)
- termite (terminal)
- transmission (torrent client)
- zsh (shell)

## Installing

To install, just run:

```
git clone https://github.com/fplucas/dotfiles.git
cd dotfiles
./install.sh
```
